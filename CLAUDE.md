# CLAUDE.md — Gym Tracker

> **Este archivo es solo un puntero.** La fuente única de verdad del proyecto
> (reglas de operación, arquitectura, schema, deploy, lecciones, historial) es
> **`README.md`**. Léelo COMPLETO antes de tocar cualquier archivo — es
> obligatorio para todo agente, no solo Claude.

Reglas mínimas si aún no leíste el README (léelo igual):

1. `npm test` y `npm run check` verdes antes de cada commit.
2. Bumpear `CACHE` en `sw.js` en cada deploy; archivos nuevos → `ASSETS`.
3. Registrar el cambio en README §9 en el mismo commit.
4. Solo sets `Done` cuentan para PR/volumen. Peso canónico en kg.
5. Sin dependencias externas. Sin `innerHTML`. Sin refactors de paso.
